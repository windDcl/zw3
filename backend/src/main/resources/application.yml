spring:
  application:
    name: gov-qa-backend
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/gov_qa?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai}
    username: ${SPRING_DATASOURCE_USERNAME:root}
    password: ${SPRING_DATASOURCE_PASSWORD:12345678}
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
    open-in-view: false

server:
  port: ${SERVER_PORT:8080}

app:
  auth:
    token-ttl-minutes: ${APP_AUTH_TOKEN_TTL_MINUTES:720}
  nlp:
    base-url: ${APP_NLP_BASE_URL:http://localhost:8000}
    timeout-ms: ${APP_NLP_TIMEOUT_MS:5000}
  graph:
    enabled: ${APP_GRAPH_ENABLED:false}
    uri: ${APP_GRAPH_URI:bolt://localhost:7687}
    username: ${APP_GRAPH_USERNAME:neo4j}
    password: ${APP_GRAPH_PASSWORD:12345678}
    database: ${APP_GRAPH_DATABASE:neo4j}
    max-neighbors: ${APP_GRAPH_MAX_NEIGHBORS:10}

logging:
  level:
    root: INFO
    com.govqa: INFO
